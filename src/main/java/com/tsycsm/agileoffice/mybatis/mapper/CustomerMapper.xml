<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Customer">
	<insert id="insert" parameterType="Customer">
		insert into customer(customer_id, owner_id, customer_name, phone)
		values(seq_customer.nextval, #{owner_id}, #{customer_name}, #{phone})
	</insert>
	
	<select id="select" parameterType="String" resultType="Customer">
		select * from customer where phone=#{phone}
	</select>
	
	<select id="selectById" parameterType="int" resultType="Customer">
		select * from customer where customer_id=#{customer_id}
	</select>
	
</mapper>








