<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Category">

	<select id="selectAll" resultType="Category">
		select * from Category order by Category_id desc
	</select>

	<select id="selectByOwner" parameterType="int" resultType="Category">
		select * from category order by category_id desc where owner_id=#{owner_id}
	</select>
	
	<insert id="insert" parameterType="Category">
		insert into category(category_id, owner_id, category_name, color)
		values(seq_category.nextval, #{owner_id}, #{category_name}, #{color})
		<selectKey keyProperty="category_id" resultType="int" order="AFTER">
			select seq_category.currval from dual
		</selectKey>
	</insert>
	
</mapper>

